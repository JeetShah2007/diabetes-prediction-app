# diabetes-prediction-app
A diabetes risk prediction app with a Random Forest ML model (Python), Express backend, and React frontend. Includes prediction history, feature importance, and visual range indicators.
